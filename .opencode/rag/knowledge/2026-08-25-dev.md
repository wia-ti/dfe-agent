# Aprendizados -- dev -- 2026-08-25

> Extraido automaticamente de transcript via .opencode/rag/summarize.ts
> Total de blocos significativos: 7
> Categorias cobertas: 2/4
>
> **Sprint 13 (I13.2)**: arquivo reclassificado. Originalmente emitido com
> slug `backend-engineer` (pre-Sprint 11), renomeado para slug canonico
> `dev` (Sprint 10 consolidou `@dev` como owner unico). Conteudo semantico
> preservado (causa raiz + fix do `download_pending`); apenas metadata
> canonica atualizada.

## Bugs resolvidos com causa raiz

- Investigue src/collector/downloader.py::DocumentCollector.discover_and_register. Tem um bug de resiliência: se um portal cai, a varredura inteira aborta.

## Decisoes de arquitetura e o porquê

_Nenhum aprendizado desta categoria no transcript._

## Padroes adotados pelo time

- BUG_ROOT_CAUSE: discover_and_register iterava PORTAL_URLS sem try/except, entao um unico RequestException em discover_documents (timeout/DNS/5xx) abortava o loop e impedia os demais portais de serem varridos naquela execucao.
- DECISION: isolar cada source com try/except requests.RequestException no corpo do for, logar em stderr (mesmo padrao de _run_dry_run) e seguir para o proximo source — sem tocar em portal_index.py nem mudar a semantica de retorno.
- TEAM_PATTERN: reforcei a convencao ja presente no projeto de 'try/except por item, nao global' usada em download_pending (linhas 130-139), e o estilo de logging em stderr com prefixo [<source>] ja estabelecido em __main__.py:64.
- DIDNT_WORK: primeira execucao da suite quebrou por falta do pacote pytest-mock no venv (fixture 'mocker' nao registrada); instalei pytest-mock 3.15.1 e re rodei — 42/42 verdes. Collection errors em tests/unit/indexer/ e tests/unit/query/ sao pre-existentes (conflito huggingface_hub) e estao fora do escopo do collector.
- FOLLOW-UP: descobrir_documentos tambem pode levantar erros de parsing BeautifulSoup (response.text malformado, encoding quebrado); hoje esses NAO sao capturados pelo except RequestException. Vale um try/except mais amplo (ou um catch dedicado) numa task separada.
- ARQUIVOS_ALTERADOS: src/collector/downloader.py (fix + import sys); tests/unit/collector/test_downloader.py (+2 testes). CATEGORIA: bug_root_cause.

## O que nao funcionou e por quê

_Nenhum aprendizado desta categoria no transcript._
